import React from "react";
import { Button, Container, Input, InputContent, Label, RadioGroup } from "./style";

const Form = () => {
  return (
    <Container>
      <InputContent>
      <Label />
      <RadioGroup>
      </RadioGroup>
      <Input />
      <Input />
      </InputContent>
      <Button />
    </Container>
  );
};

export default Form;