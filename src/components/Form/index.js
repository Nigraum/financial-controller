import React from "react";
import { Container, Input, InputContent, Label, RadioGroup } from "./style";

const Form = () => {
  return (
    <Container>
      <InputContent>
      <Label />
      <RadioGroup>
      </RadioGroup>
      <Input />
      <Input />
      </InputContent>
    </Container>
  );
};

export default Form;