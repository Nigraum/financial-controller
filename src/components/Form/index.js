import React from "react";
import { Container, Input, InputContent, Label } from "./style";

const Form = () => {
  return (
    <Container>
      <InputContent>
      <Label />
      <Input />
      <Input />
      </InputContent>
    </Container>
  );
};

export default Form;